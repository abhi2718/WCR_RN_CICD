5. Need to discuss with 
@Shazia
 After login with Apple if user presses the back button and tries with google edge case
 signinViewModal line number 96


Notes for Junior Developer 

Homebrew is a package manager for macOS (and Linux, too). It is one of the first tools you'll need to set up a local development environment for programming on a Mac. Use it to install (and remove) software programs for the terminal, or command line.

Ruby is language used for desktop applications, devOps 
RubyGems is a package manager for Ruby.

What is CocoaPods used in iOS?
Cocoapod is a package dependency manager for Objective-C and Swift projects with XCode. Those Languages are used to make applications that run iOS, macOS… Cocoapod is a package dependency manager for Objective-C and Swift projects with XCode.
