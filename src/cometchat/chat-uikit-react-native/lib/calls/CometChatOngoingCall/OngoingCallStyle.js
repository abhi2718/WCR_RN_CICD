export class OngoingCallStyle {
    minWidth;
    minHeight;
    maxWidth;
    maxHeight;
    constructor({ minWidth, minHeight, maxWidth, maxHeight, }) {
        this.minWidth = minWidth;
        this.minHeight = minHeight;
        this.maxWidth = maxWidth;
        this.maxHeight = maxHeight;
    }
}
//# sourceMappingURL=OngoingCallStyle.js.map